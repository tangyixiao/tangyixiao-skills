# 网络协议可视化 Prompt 参考

---

## 基础生成

```
生成网页模拟{协议名称}（{简短描述}），要求完成度高，800行以上代码
```

```
生成网页{协议名称}，使用偏暗的色调，
主要注重图形与动态显示效果，在一个屏幕内显示，
要求完成度高，1000行以上代码
```

---

## 数据包/帧结构展示

```
生成纯前端页面演示{协议}数据包结构，使用偏暗的炫酷色调，
以分层矩形块展示各字段，鼠标hover显示字段说明，
多一些图形效果，要求1000行以上代码
```

```
为该动画添加3D效果
```

```
为X模块整体添加3D旋转效果，实现鼠标往页面左侧移动时该模块Y轴向左，
鼠标往页面右侧移动时该模块Y轴向右
```

---

## 交互流程演示（握手/协商）

```
生成{协议}交互流程可视化，客户端/服务端左右布局，
用带箭头的时序图展示，数据包在两端流动，每步附说明文字
```

```
实现逐步演示，支持点击"下一步"推进流程，
并提供自动播放和重置功能
```

---

## 路由/转发演示

```
生成网络路由演示，包含拓扑图（4-6个节点）、路由表、
数据包沿路径流动动画，支持输入目标IP查看路由决策过程
```

---

## 安全/防御演示

```
生成网页模拟防火墙基于IP地址与端口过滤流量，
要求完成度高，800行以上代码
```

```
生成一个关于{攻击类型}的安全教育演示页面，
仅展示原理，不涉及具体攻击代码，底部加免责声明
```

---

## 动画优化

```
为数据包流动箭头添加持续循环的流动效果，
使箭头在完成初始出现动画后能够保持动态状态
```

```
帮我实现在打开这个网页时，各个元素依次"缓入"出现的动画效果，
且在切换步骤时元素内容变化时也添加变化的动画效果
```

```
为所有协议层节点添加鼠标经过后"发光强调"的效果，
并设置为默认持续发光
```
